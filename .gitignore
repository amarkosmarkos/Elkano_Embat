# dependencies
node_modules/
.pnp.*
.pnpm-store/

# next.js
.next/
out/
build/
dist/

# env
.env
.env.local
.env*.local

# misc
*.log
.DS_Store
*.tsbuildinfo
next-env.d.ts
ssh_key.txt

# datos crudos del reto (646 MB, se descomprimen de output_hackspain_data.zip) y salidas del
# pipeline (output/0{1,2,3}_*, regenerables con ./pipeline/run.sh, ~1 min)
output
output/

# data/*.json SÍ va al repo: es el contrato de docs/CONTRATO_DATOS.md que carga
# apps/web/scripts/seed.ts en Postgres — no es una salida regenerable del pipeline real.

# derivados del EDA (se regeneran con ./eda/run.sh)
eda/panel.csv
eda/company_trend_features.csv
